# TraceNet v2 — Environment Variables Template
# Copy this to .env and fill in values
# .env is gitignored — never commit the real .env

# Model file paths (defaults work if running from project root)
MODEL_PATH=models/gnn_model.pth
CONFIG_PATH=models/model_config.json
DATA_PATH=models/processed_data.npz

# Set by Railway automatically — do not set manually on Railway
# PORT=8000
